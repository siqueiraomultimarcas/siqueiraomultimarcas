[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
# A porta vem de gunicorn.conf.py (lê PORT em Python), não da linha de comando
cmd = "gunicorn -c gunicorn.conf.py app:app"
